{
  "name": "lull",
  "displayName": "lull",
  "description": "Earn while AI thinks. Quiet sponsor cards during AI wait states.",
  "version": "0.1.0",
  "publisher": "lull",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/lull-app/lull-extension.git"
  },
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "icon": "images/lull-icon.png",
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "lull-sidebar",
          "title": "lull",
          "icon": "images/lull-icon.svg"
        }
      ]
    },
    "views": {
      "lull-sidebar": [
        {
          "type": "webview",
          "id": "lull.adView",
          "name": "lull"
        }
      ]
    },
    "commands": [
      {
        "command": "lull.resetId",
        "title": "lull: Reset my ID"
      },
      {
        "command": "lull.simulateAd",
        "title": "lull: Simulate AI call (show ad)"
      },
      {
        "command": "lull.simulateIdle",
        "title": "lull: Simulate idle state"
      },
      {
        "command": "lull.openAdUrl",
        "title": "lull: Open sponsor link"
      },
      {
        "command": "lull.restoreSettings",
        "title": "lull: Remove lull from Claude settings"
      }
    ],
    "configuration": {
      "title": "lull",
      "properties": {
        "lull.backendUrl": {
          "type": "string",
          "default": "",
          "description": "lull backend URL. Leave empty to use built-in demo ads."
        },
        "lull.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Enable or disable lull."
        }
      }
    }
  },
  "scripts": {
    "build": "node esbuild.config.js",
    "dev": "node esbuild.config.js --watch",
    "package": "vsce package --no-dependencies"
  },
  "devDependencies": {
    "@types/vscode": "^1.85.0",
    "@vscode/vsce": "3.9.2",
    "esbuild": "^0.20.0",
    "typescript": "^5.3.0"
  }
}
